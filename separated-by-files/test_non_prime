import math
def isprime(num):
    if num < 2:
        return False
    for i in range(2, (int(math.sqrt(num))) + 1):
        if num % i == 0:
            return False
    return True
non_primes = [n for n in range(21, 30) if not isprime(n)]
print(non_primes)